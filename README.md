# StockMarket_Predection
This project provides a complete setup for a Stock Market Price Prediction mini-project. It uses a Long Short-Term Memory (LSTM) neural network, which is the industry standard for time-series data because it can "remember" past price patterns.
